# ==============================================================================
# Calamares — userpkglist module config for NooreldeanOS
# ==============================================================================
# This module is referenced in settings.conf but Calamares supports it
# without config when the user doesn't customize packages during install.
---
